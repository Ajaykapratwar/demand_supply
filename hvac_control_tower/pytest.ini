[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: Unit tests (per service)
    integration: Integration tests (layer pairs)
    contract: Contract compatibility tests
    scenario: Canonical scenario tests
    slow: Tests that take >30s
